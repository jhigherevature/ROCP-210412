package qualifiedname;

import resources.Data;

public class Simulator {
	public static void main(String[] args) {
		/*
		 * 'Qualifier' refers to what location a resource 
		 * is coming from.
		 * 
		 * The fully qualified name in Java is the term
		 * used when specifying a datatype of class using
		 * the package structure prior to the class's name 
		 */
		informational.Data d = new informational.Data();
		
		Data rd = new Data();	
	}
}
